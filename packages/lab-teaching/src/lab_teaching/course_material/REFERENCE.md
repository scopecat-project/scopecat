# 参考解与检查标准

请先完成 Notebook 的局部改动与观察，再核对这里。

- 第 1 题：频率改成 `5.148`；差异应只包含 `teaching_drive` 中 `q0` 的 `frequency`。
- 第 2 题：`amplitudes = np.linspace(0, 0.8, 21)`；`request.values["amplitude"] = sc.Scan(amplitudes)`，预览为 21 点。可选题通过 `request.copy()` 创建独立副本，修改 seed 或扫描范围不影响原请求。
- 第 3 题：均值 I 呈振荡，教学拟合 `passed`，候选约 `0.24 arb`。
  频率偏移会影响响应强度，但本题不训练频率标定。
- 第 4 题：保存之后重新打开对应版本应保留 `5.148`；重开原 run 不增加采集。
- 第 5 题：窄范围不能给出通过候选。改回 `np.linspace(0, 0.8, 21)` 后重新采集，再查看图和诊断。
- 第 6 题：新 run ID、不同噪声、近似候选。两次结果相差小于 `0.02 arb`；这不是接受参数的动作。
- 第 7 题：全零平线，`fit_failed`，`pi_amplitude is None`。下一步检查是否存在响应，不强行接受拟合。

如果结果不符合这里，不要修改报告值。保存 run ID、图、错误和所改输入，帮助定位科学条件或实现问题。
本轮没有自动将拟合候选写入参数表，也没有将任何版本发布为实验室默认。
