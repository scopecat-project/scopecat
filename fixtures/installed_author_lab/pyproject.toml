[project]
name = "scopecat-pilot-methods"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["scopecat"]

[build-system]
requires = ["uv_build>=0.12.3,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "pilot_methods"
